#include "main.h"

/**
 * add - add two numbers
 * @a: the first number
 * @b: the secondn number
 * Return value of of add is both a and b
 * Returns: the addition of both
 */
int add(int a, int b)
{
	return (a + b);
}

